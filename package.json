{
    "name": "fsmat-at",
    "private": true,
    "scripts": {
        "start": "concurrently \"yarn workspace cms start\" \"yarn workspace frontend start\"",
        "dev": "concurrently \"yarn workspace cms dev\" \"yarn workspace frontend dev\"",
        "build": "concurrently \"yarn workspace cms build\" \"yarn workspace frontend build\"",
        "format": "prettier --write ."
    },
    "workspaces": [
        "packages/*"
    ],
    "packageManager": "yarn@4.4.1",
    "devDependencies": {
        "concurrently": "^9.1.2",
        "prettier": "^3.5.3"
    }
}
